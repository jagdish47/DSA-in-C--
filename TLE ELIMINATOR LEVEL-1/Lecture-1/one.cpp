#include <iostream>
using namespace std;

int main()
{

    // int a, b, c;
    // cin >> a >> b >> c;

    // If your using string you can not use space when you use space it will consider as new string
    // Hello how are
    // a -> Hello
    // b -> how
    // c -> you
    string a, b, c;
    cin >> a >> b >> c;

    cout << a << endl;
    cout << b << endl;
    cout << c << endl;

    return 0;
}

/**
 * Operator
 * Datatype
 * Input
 * Output
 * if-else -> statements
 * Type Conversion
 *
 */