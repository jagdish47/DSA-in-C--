#include <bits/stdc++.h>
using namespace std;

int main()
{

    array<int, 5> arr = {2, 16, 77, 34, 50};
    // to use array method must have to declare array as classname
    int *ptr = arr.begin();
    cout << *ptr << endl;
    // cout << arr.begin() << endl;
    // cout << arr[0] << endl;
    return 0;
}